example:
  property: I AM THE PROD
management:
  endpoint:
    shutdown:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "beans,caches,conditions,configprops,env,health,httpexchanges,info,loggers,metrics,mappings,sessions,shutdown,prometheus"
spring:
  datasource:
    url: jdbc:mysql://x.x.x.x:3306/spring_microservices
    username: admin
    password: 123456789